using UnityEngine;

public class TestingZone : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Grid grid = new Grid(4, 2, 10.0f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
